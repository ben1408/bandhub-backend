# Environment Configuration
# Copy this file to .env and fill in your actual values
# NEVER commit .env to version control!

# Environment
NODE_ENV=development  # or 'production' for production

# Server Port
PORT=5050

# MongoDB Connection
# For local development:
# MONGO_URI=mongodb://127.0.0.1:27017/musicdb
# For MongoDB Atlas (production):
MONGO_URI=mongodb+srv://username:password@cluster.mongodb.net/bandhub?retryWrites=true&w=majority

# JWT Secret - MUST BE STRONG AND RANDOM IN PRODUCTION
# Generate a secure secret: https://generate-secret.vercel.app/32
# Or use: openssl rand -base64 32
# CRITICAL: Use different secrets for dev and production!
JWT_SECRET=your_jwt_secret_here_MINIMUM_32_CHARACTERS_CHANGE_THIS

# OpenAI API Key (optional - only needed for poster generation feature)
OPENAI_API_KEY=your_openai_api_key_here

# Frontend URL
# Local development:
FRONTEND_URL=http://localhost:5173
# Production (Vercel):
# FRONTEND_URL=https://your-app.vercel.app

# Security Notes:
# - Never use the same JWT_SECRET in dev and production
# - Keep this file in .gitignore
# - Rotate secrets regularly
# - Use environment variables in deployment platforms (Render, Vercel)
